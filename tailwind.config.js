/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./web/templates/**/*.html",
  ],
  darkMode: 'class', // Use class-based dark mode instead of media query
  theme: {
    extend: {},
  },
  plugins: [],
}
